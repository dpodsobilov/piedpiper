import styled from "styled-components";
import logo from '../../../asserts/header/logo.svg'

export const HeaderLogo = styled.div`
  background: url(${logo}) no-repeat;
  background-size: contain;
  margin: 0.5em;
  width: 15%;
  height: 80%;
  cursor: pointer;
`;